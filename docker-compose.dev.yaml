version: '3'
services:
  backend:
    build:
      dockerfile: Dev.Dockerfile
      context: .
    volumes:
    - ./:/deno
    env_file:
      - .env
    ports:
    - 8000:8000
    restart: unless-stopped
